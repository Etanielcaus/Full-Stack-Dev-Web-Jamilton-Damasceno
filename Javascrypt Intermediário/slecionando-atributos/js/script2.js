function executar(){
    console.log("Executar");
}

//window.addEventListener("load", executar); //NOTE - Ele executa quando a tela é carregada
//window.addEventListener("unload", executar); //NOTE - Ele executa só quando a tela é encerrada
//window.addEventListener('error', executar); //NOTE - Ele chama a mensagem de erro e executa
//window.addEventListener('resize', executar); //NOTE - Ele executa quando a tela é redimensionada
//window.addEventListener('scroll', executar); //NOTE - Só executa quando a página é scrollada
